diff --git a/src/legacy.py b/src/legacy.py
--- a/src/legacy.py
+++ b/src/legacy.py
@@ -1,30 +1,3 @@
-# Deprecated legacy connection utilities
-import socket
-
-LEGACY_HOST = "localhost"
-LEGACY_PORT = 8080
-LEGACY_TIMEOUT = 30
-
-
-def legacy_connect(host, port, timeout):
-    s = socket.socket()
-    s.settimeout(timeout)
-    s.connect((host, port))
-    return s
-
-
-def legacy_send(sock, data):
-    return sock.send(data.encode())
-
-
-def legacy_recv(sock, size=1024):
-    return sock.recv(size).decode()
-
-
-def legacy_close(sock):
-    sock.close()
-
-
-def legacy_ping(host=LEGACY_HOST, port=LEGACY_PORT):
-    sock = legacy_connect(host, port, LEGACY_TIMEOUT)
-    legacy_send(sock, "PING")
+from transport import connect, ping
+
+PING = ping

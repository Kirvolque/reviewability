diff --git a/src/processor.py b/src/processor.py
--- a/src/processor.py
+++ b/src/processor.py
@@ -1,5 +1,5 @@
-def process(data):
-    result = []
-    for item in data:
-        result.append(item)
-    return result
+def process(data: list) -> list:
+    validate(data)
+    items = [transform(item) for item in data]
+    log(f"processed {len(items)} items")
+    return items
@@ -15,5 +15,5 @@
-def flush(queue):
-    queue.clear()
-    stats["flushed"] += 1
-    stats["last_flush"] = time.time()
-    return None
+def flush(queue: list) -> None:
+    count = len(queue)
+    queue.clear()
+    log(f"flushed {count} items")
+    stats["flushed"] += count
diff --git a/src/validator.py b/src/validator.py
--- a/src/validator.py
+++ b/src/validator.py
@@ -1,4 +1,4 @@
-def validate(data):
-    if not data:
-        return False
-    return True
+def validate(data: dict) -> bool:
+    if not isinstance(data, dict):
+        raise TypeError(f"expected dict, got {type(data).__name__}")
+    return bool(data)
@@ -15,4 +15,4 @@
-def is_valid_email(s):
-    return "@" in s and "." in s.split("@")[-1]
-    # TODO: use regex
-    return False
+def is_valid_email(address: str) -> bool:
+    import re
+    pattern = r"^[^@\s]+@[^@\s]+\.[^@\s]+$"
+    return bool(re.match(pattern, address))
diff --git a/src/formatter.py b/src/formatter.py
--- a/src/formatter.py
+++ b/src/formatter.py
@@ -1,6 +1,6 @@
-def format_output(data):
-    lines = []
-    for k, v in data.items():
-        lines.append(f"{k}={v}")
-    return "\n".join(lines)
-
+def format_output(
+    data: dict,
+    separator: str = "\n",
+    key_value_sep: str = "=",
+) -> str:
+    return separator.join(f"{k}{key_value_sep}{v}" for k, v in data.items())
